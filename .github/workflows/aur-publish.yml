name: Publish AUR

on:
  workflow_dispatch:
    inputs:
      tag:
        description: "Version tag to publish (e.g. v1.2.0)"
        required: true
        type: string

permissions:
  contents: write

jobs:
  aur-publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0 # full history (commit-to-main rebases onto origin/main)

      - name: Resolve version tag
        id: version
        env:
          INPUT_TAG: ${{ inputs.tag }}
        run: |
          set -euo pipefail
          TAG="$INPUT_TAG"
          if ! [[ "$TAG" =~ ^v[0-9]+\.[0-9]+\.[0-9]+$ ]]; then
            echo "::error::Invalid tag format: $TAG (expected vX.Y.Z)"
            exit 1
          fi
          VERSION="${TAG#v}"
          echo "tag=$TAG" >> "$GITHUB_OUTPUT"
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Update PKGBUILD pkgver
        working-directory: packaging
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          sed -i "s/^pkgver=.*/pkgver=${VERSION}/" PKGBUILD

      - name: Copy bundled files with versioned names
        working-directory: packaging
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"
          PKGNAME=$(grep -oP '^pkgname=\K.*' PKGBUILD)
          cp ../docs/odm.1 "${PKGNAME}-${VERSION}.1"
          cp ../configs/odm.conf.example "${PKGNAME}-${VERSION}.conf.example"
          cp ../packaging/odm.service "${PKGNAME}-${VERSION}.service"
          cp ../LICENSE "${PKGNAME}-${VERSION}.LICENSE"

      - name: Get binary checksum and update PKGBUILD
        working-directory: packaging
        run: |
          set -euo pipefail
          VERSION="${{ steps.version.outputs.version }}"

          curl -fsSL -o /tmp/checksums.txt \
            "https://github.com/${{ github.repository }}/releases/download/v${VERSION}/checksums.txt"

          I686_SUM=$(awk -v file="odm_${VERSION}_linux_386" '$2 == file { print $1; exit }' /tmp/checksums.txt)
          AMD64_SUM=$(awk -v file="odm_${VERSION}_linux_amd64" '$2 == file { print $1; exit }' /tmp/checksums.txt)
          ARMV7H_SUM=$(awk -v file="odm_${VERSION}_linux_arm" '$2 == file { print $1; exit }' /tmp/checksums.txt)
          ARM64_SUM=$(awk -v file="odm_${VERSION}_linux_arm64" '$2 == file { print $1; exit }' /tmp/checksums.txt)

          if [ -z "$I686_SUM" ] || [ -z "$AMD64_SUM" ] || [ -z "$ARMV7H_SUM" ] || [ -z "$ARM64_SUM" ]; then
            echo "::error::Missing binary checksum in release artifacts"
            exit 1
          fi

          export I686_SUM AMD64_SUM ARMV7H_SUM ARM64_SUM
          python3 ../scripts/update-aur-checksums.py

      - name: Verify checksums and regenerate .SRCINFO
        working-directory: packaging
        run: |
          set -euo pipefail
          docker run --rm -v "$PWD:/pkg" -w /pkg archlinux:base-devel bash -c "
            useradd -m builder
            chown -R builder:builder /pkg
            su builder -c 'makepkg --verifysource --nodeps'
            su builder -c 'makepkg --printsrcinfo' > .SRCINFO
          "

      - name: Validate
        working-directory: packaging
        run: |
          set -euo pipefail
          errors=0
          grep -q "^pkgname=odm-bin$" PKGBUILD || { echo "::error::Missing pkgname"; errors=1; }
          grep -q "^pkgver=${{ steps.version.outputs.version }}$" PKGBUILD || { echo "::error::Wrong pkgver"; errors=1; }
          [ "$errors" -eq 0 ] || exit 1

      - name: Publish AUR
        working-directory: packaging
        env:
          AUR_SSH_PRIVATE_KEY: ${{ secrets.AUR_SSH_PRIVATE_KEY }}
          AUR_COMMIT_USERNAME: ${{ secrets.AUR_USERNAME }}
          AUR_COMMIT_EMAIL: ${{ secrets.AUR_EMAIL }}
          VERSION: ${{ steps.version.outputs.version }}
        # Custom sync script (replaces ulises-jeremias/github-actions-aur-publish):
        # prunes stale odm-bin-<ver>.* assets from the AUR repo before copying
        # the current set, so old-version files never accumulate.
        run: bash ../scripts/aur-sync.sh "$VERSION"

      - name: Clean up generated assets
        working-directory: packaging
        # makepkg (run as 'builder' in the archlinux container) chowns the whole
        # workdir, so the plain runner can't remove the versioned files — sudo.
        run: sudo rm -f odm-bin-*.*

      - name: Commit to main
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git remote set-url origin "https://x-access-token:${GITHUB_TOKEN}@github.com/${{ github.repository }}.git"
          git fetch origin main
          # Rebase the PKGBUILD edits onto the latest main, then push
          # explicitly. A bare `git push` only works on a branch checkout; the
          # default checkout here is detached, so an explicit branch+refspec is
          # required to land the commit on main.
          git checkout -B aur-publish origin/main
          git add packaging/PKGBUILD
          if ! git diff --cached --quiet; then
            git commit -m "chore(aur): update PKGBUILD for v${{ steps.version.outputs.version }}"
            git push --force-with-lease origin aur-publish:main
          else
            echo "No changes to commit"
          fi
